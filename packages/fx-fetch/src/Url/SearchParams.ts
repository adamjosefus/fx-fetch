/**
 * @internal Type representing HTTP search parameters as a read-only map with lowercase keys and readonly string array values.
 */
export type SearchParams = ReadonlyMap<string, readonly string[]>;
