{
  "private": true,
  "scripts": {
    "build": "nx build fx-fetch",
    "fmt": "biome check . --error-on-warnings && pnpm local:fmt",
    "fmt:write": "nx run-many --target=fmt:write && pnpm local:fmt:write",
    "local:fmt": "biome check . --error-on-warnings",
    "local:fmt:write": "biome check . --write --error-on-warnings",
    "prepare": "husky",
    "test": "nx run-many --target=test",
    "validate": "nx run-many --target=validate",
    "website": "nx build website"
  },
  "dependencies": {},
  "devDependencies": {
    "@/shared": "workspace:*",
    "@biomejs/biome": "^2.3.11",
    "@nx/js": "^22.3.3",
    "@swc-node/register": "~1.11.1",
    "@swc/core": "~1.15.8",
    "@swc/helpers": "~0.5.18",
    "husky": "^9.1.7",
    "nx": "^22.3.3"
  }
}